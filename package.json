{
  "name": "apollon-monorepo",
  "private": true,
  "workspaces": [
    "sdk/typescript",
    "frontend/apollon-dashboard"
  ],
  "scripts": {
    "build:sdk": "npm run build --workspace=@apollon/near-sdk",
    "build:frontend": "npm run build --workspace=apollon-oracle-dashboard",
    "build": "npm run build:sdk && npm run build:frontend"
  }
}
